/**
 * Re-export shared presentation helpers from the Next.js lib so the Edge
 * Function bundle and the Statement Lines UI stay on the same labels.
 */
export * from "../../../src/lib/bank/statement-report-format.ts";
